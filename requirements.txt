# =========================
# PROCESAMIENTO DE AUDIO (Notebook 1)
# =========================
librosa            # carga y procesamiento de audio
pydub              # manipulación de formatos de audio
soundfile          # lectura/escritura de archivos de audio
scipy              # funciones de procesamiento de señal
noisereduce        # reducción de ruido
silero-vad         # modelo de VAD preentrenado

# =========================
# ASR (Notebook 2)
# =========================
faster-whisper     # modelo ASR optimizado (Whisper)
jiwer              # métricas de evaluación (WER, CER)
tqdm               # barra de progreso
num2words          # conversión de números a palabras

# =========================
# NLP (Notebook 3)
# =========================
spacy              # procesamiento de lenguaje natural
transformers       # modelos preentrenados (BERT, etc.)
sentence-transformers  # embeddings semánticos
scikit-learn       # modelos y utilidades ML
datasets           # gestión de datasets
evaluate           # evaluación de modelos NLP
unidecode          # normalización controlada (acentos, ruido)
rapidfuzz          # matching difuso (plagas, productos, variantes)
joblib             # persistencia de modelos sklearn

# =========================
# DEPENDENCIAS CORE (ML / DL)
# =========================
torch              # backend para modelos de deep learning (ASR y NLP)
numpy              # operaciones numéricas
pandas             # manejo de datos tabulares

# =========================
# VISUALIZACIÓN
# =========================
matplotlib         # visualización base
seaborn            # visualización avanzada